<div class="container">
        <div class="card-deck text-center d-inline-block" *ngFor="let faction of factions">
            <div class="row">
                <div class="card">
                    <div class="card-body">
                    <img src="{{faction.image}}" width="300" height="300">
                    <h2>{{faction.faction}}</h2>
                    <h5>{{faction.quote}}</h5>
                    <button class="btn btn-primary" [routerLink]="[ faction._id]">Details</button>
                    </div>
                </div>
            </div>
        </div>
    </div>